import { CheckUpdateRoleGuard } from './check-update-role.guard';

describe('CheckUpdateRoleGuard', () => {
  it('should be defined', () => {
    expect(new CheckUpdateRoleGuard()).toBeDefined();
  });
});
